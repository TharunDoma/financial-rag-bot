# ============================================================
#  .gitignore — Protect secrets and keep the repo clean
# ============================================================

# NEVER commit your API keys
.env

# Python cache files
__pycache__/
*.pyc
*.pyo

# Virtual environment — never commit this
venv/
.venv/
